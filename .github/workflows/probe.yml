name: Langpack probe

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-22.04
    steps:
      - name: Probe download.moodle.org
        run: |
            curl -sSIL --max-time 30 https://download.moodle.org/langpack/4.5/languages.md5 | grep -E '^HTTP/' || echo "UNREACHABLE"
